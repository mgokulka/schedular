import { ShortDatePipePipe } from './short-date-pipe.pipe';

describe('ShortDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
